import { TonClient } from '@ton/ton';
interface BuyParams {
    tonClient: TonClient;
    jettonAddress: string;
    mnemonics: string[];
    tonAmount: string;
    slippage?: number;
}
interface SellParams {
    tonClient: TonClient;
    jettonAddress: string;
    mnemonics: string[];
    jettonAmount: string;
    slippage?: number;
}
export declare function buy({ tonClient, jettonAddress, mnemonics, tonAmount, slippage }: BuyParams): Promise<bigint>;
export declare function sell({ tonClient, jettonAddress, mnemonics, jettonAmount, slippage }: SellParams): Promise<bigint>;
export {};
